function getRandomHexColor() {
  return `#${Math.floor(Math.random() * 16777215)
    .toString(16)
    .padStart(6, 0)}`;
}

// Це зроблю у день питання-відповидь, зараз часу взагалі нема нажаль ні на що :(
